name: First Workflow  # Это название будет отображаться на вкладке Actions

on: workflow_dispatch
# on: push

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display some Text
        run: echo "Some Text"
      - name: Command in Multiline
        run: |
          node -v
          npm -v